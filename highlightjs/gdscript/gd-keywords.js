
class GDKeywords{

    static keywords = "and in signal await not or self void as assert breakpoint class class_name extends is func setget tool yield const enum export onready static var remote sync master puppet remotesync mastersync puppetsync Color8 ColorN abs asin assert atan atan2 bytes2var cartesian2polar ceil char clamp convert cos cosh db2linear decimals dectime deg2rad dict2inst ease exp floor fmod fposmod funcref get_stack hash inst2dict instance_from_id inverse_lerp is_equal_approx is_inf is_instance_valid is_nan is_zero_approx len lerp lerp_angle linear2db load log max min move_toward nearest_po2 ord parse_json polar2cartesian posmod pow preload print print_debug print_stack printerr printraw prints printt push_error push_warning rad2deg rand_range rand_seed randf randi randomize range range_lerp round seed sign sin sinh smoothstep sqrt step_decimals stepify str str2var tan tanh to_json type_exists typeof validate_json var2bytes var2str weakref wrapf wrapi yield PI TAU INF NAN true false null"

    static controlFlowKeywords = "if elif else for match break continue while pass return"

    static baseTypes = "String Array Dictionary Vector2 Vector3 Rect2 PoolByteArray PoolColorArray PoolIntArray PoolRealArray PoolStringArray PoolVector2Array PoolVector3Array int float"

    static engineTypes = "Control Node Node3D Node2D ConfirmationDialog RefCounted Button EditorInterface FileDialog"

    static builtInNames = "print range preload"

    //functionDefinitions = "_enter_tree _exit_tree _get _get_configuration_warning _get_property_list _init _input _notification _physics_process _process _ready _set _to_string _unhandled_input _unhandled_key _draw _clips_input _get_minimum_size _gui_input _make_custom_tooltip"
}

export default GDKeywords;
